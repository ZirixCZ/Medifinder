import {StyleSheet, Text, View, Button} from "react-native";

const PokusScreen = () => {
    return (
        <View style={{flex: 1, alignItems: "center", justifyContent: "center"}}>
            <Text>Pokus Screen</Text>
        </View>
    );
};

export default PokusScreen;
